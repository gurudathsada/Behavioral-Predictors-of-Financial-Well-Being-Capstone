# Outputs Folder

This folder contains final-run figures, tables, and logs used to support the capstone analysis. Trained model binaries are intentionally excluded because the code and public data can regenerate them.
