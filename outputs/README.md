# Outputs Folder

This folder contains final-run figures, tables, and logs used to support the final report. Trained model binaries are intentionally excluded from the clean GitHub upload because the code and public data can regenerate them.
